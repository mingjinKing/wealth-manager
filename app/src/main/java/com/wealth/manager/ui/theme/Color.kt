package com.wealth.manager.ui.theme

import androidx.compose.ui.graphics.Color

// Primary gold accent
val Primary = Color(0xFFffc880)
val PrimaryDark = Color(0xFF0c0c1f)

// Light theme: white background with dark cards
val Background = Color(0xFFFFFFFF)
val Surface = Color(0xFFF5F5F5)        // Light gray cards
val SurfaceElevated = Color(0xFFFFFFFF) // Pure white elevated surfaces

// Semantic colors (same as before)
val Safe = Color(0xFF4A90D9)
val Warning = Color(0xFFE55B5B)
val Income = Color(0xFF4CAF50)         // Income green

// Text colors
val OnPrimary = Color(0xFF0c0c1f)
val OnBackground = Color(0xFF1A1A2E)    // Dark text on white
val OnSurface = Color(0xFF1A1A2E)       // Dark text on cards
val TextSecondary = Color(0xFF9E9EA4)

// Divider / outline
val Divider = Color(0xFFE0E0E0)
val Outline = Color(0xFFE0E0E0)
