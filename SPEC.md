# 消费透视镜 (Consumer Telescope) - 项目规格说明书

> V1 聚焦版 —— 纯工具定位
> 版本：v2.0 | 更新：2026-03-31 | 变更：浅色主题 + 首页重构为日账单模式 + 记账页重构 + 导航精简为3栏

---

## 1. 项目概述

**项目名称**：消费透视镜
**项目类型**：Android 原生应用
**一句话描述**：像钱迹一样方便快捷的记账工具，加上 AI 消费结构透视能力。

---

## 2. 核心功能

- **快速记账**：3秒内完成一笔记账，支持备注、日期、分类
- **日账单视图**：按天分组的消费明细，清晰看到每日收支
- **消费结构透视**：AI 优化建议（待接入 LLM）+ 哇时刻成就感
- **钱迹数据导入**：CSV 格式导入，历史数据无缝衔接（规划中）
- **本地优先存储**：SQLite + Room，隐私第一

---

## 3. 技术架构

| 层级 | 技术选型 |
|------|----------|
| **前端** | Android Native（Kotlin + Jetpack Compose） |
| **本地数据库** | SQLite（通过 Room 抽象） |
| **AI 能力** | LLM API（MiniMax/DeepSeek/阿里百炼），V1 使用已有算力资源 |
| **网络** | Retrofit + OkHttp |
| **依赖注入** | Hilt |
| **异步处理** | Kotlin Coroutines + Flow |
| **架构模式** | MVVM + 简化 Clean Architecture（2层：data + presentation）|
| **后台任务** | WorkManager（哇时刻定时计算，规划中）|

---

## 4. V1 MVP 功能列表

### 4.1 消费记录模块
- 添加消费记录（金额、分类、日期、备注）
- 消费分类管理（默认分类带 emoji 图标：餐饮/购物/交通/娱乐/居住/医疗/学习/其他）
- 消费记录按天分组展示（今日/昨日/具体日期）
- 点击条目 → 底部弹窗（修改/删除）
- 编辑消费记录（预填数据，保存为更新）

### 4.2 数据存储模块
- Room 数据库：消费记录表、分类表
- 本地数据持久化

### 4.3 AI 透视模块（V1）
- **优化建议**：LLM 生成具体可执行的省钱建议（消费透视页，待接入 API）
- AI 建议不在首页展示，统一归入「透视」页

### 4.2 第一个"哇"时刻 ⭐

**定义**：每周日向用户展示"因为消费透视镜，你本周少花了 ¥XX"。

**触发公式**：
```
savedAmount = last_4_week_avg - current_week_spend
wow_triggered = savedAmount > ¥100 AND savedAmount > last_4_week_avg × 20%
```

**详细说明**：
- `last_4_week_avg`：用户历史最近4周消费均值（滚动计算）
- `current_week_spend`：本周消费总额
- `savedAmount`：节省金额（正数=少花了）
- 触发条件：**绝对节省 > ¥100** 且 **相对节省 > 历史均值20%**
- 新用户（不足4周数据）：使用默认基准值

**哇时刻展示内容**：
- 节省金额（金色强调）
- 等价物展示（"相当于：一顿双人火锅"，≤2项）
- 成就徽章

### 4.5 基础 UI（v2.0）

| 页面 | 功能 |
|------|------|
| **首页/仪表盘** | 月支出概览 + 近7日支出 + 按天分组账单 + 哇时刻预览 |
| **记账页** | 分类emoji选择 + 备注输入 + 日期选择 + 金额键盘 |
| **透视页** | AI 优化建议列表（待 LLM 接入）|
| **成就页** | 哇时刻 + 成就列表 + 查看透视入口 |

**BottomNav 3栏**（v2.0 精简）：

| 图标 | 标签 | 页面 | 路由 |
|------|------|------|------|
| 🏠 | 首页 | 仪表盘 | `/dashboard` |
| 💡 | 透视 | AI 建议页 | `/insights` |
| 🏆 | 成就 | 哇时刻+成就 | `/achievements` |

> 添加页面（`/add`）为 startDestination，不在 BottomNav 中，通过 FAB 或 App 启动直接进入。

---

## 5. 非功能需求

- 最低支持 Android 7.0（API 24）
- 离线优先，数据本地存储
- 用户隐私数据不上传第三方服务器

---

## 6. v2.0 变更日志

| 变更项 | 变更内容 |
|--------|----------|
| 主题 | 从暗色（#0c0c1f）切换为浅色主题（背景#F0F0F0，卡片#FFFFFF）|
| 首页重构 | 从"周概览+消费结构"改为"月支出+近7日+按天分组账单" |
| AI建议归位 | 首页移除 AI 建议卡片，统一归入透视页 |
| 记账页重构 | 分类带 emoji 图标 + 备注字段 + 日期选择器 + 5列数字键盘 |
| 编辑/删除 | 点击消费条目弹出 ModalBottomSheet（修改/删除）|
| 日期支持 | 每笔账单支持选择具体日期存储 |
| 导航精简 | BottomNav 从 4栏减为 3栏（移除"添加"）|
| 启动页 | startDestination = /add（记账页），贯彻3秒记账理念 |

### 历史版本

| 版本 | 日期 | 关键变更 |
|------|------|----------|
| v1.2 | 2026-03-31 | 哇时刻公式明确、AI方案确定、颜色系统统一 |
| v1.3 | 2026-03-31 | 启动页改为添加页、BottomNav精简为3栏 |
