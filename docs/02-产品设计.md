# 💰 消费透视镜 · 产品设计

> 整理：玄枢·麒麟 | 最后更新：2026-03-31
> **版本：v1.2 — 哇时刻公式明确 + AI方案确定**

---

## 一、核心定位

> **记账的"第二眼"** —— 钱迹帮你记，消费透视镜帮你"看懂"

**一句话定位**：像钱迹一样方便快捷的记账工具，加上 AI 消费结构透视能力。

**核心价值主张**

- **透视感**：一眼看出"钱花哪去了"，不是数字是洞察
- **对照感**：有参照系，知道自己"偏没偏"
- **行动感**：不只是展示，是告诉你"可以怎么改"

---

## 二、V1 功能范围（必须跑通闭环）

### P0（MVP，只做这些）

1. **快速记账**（3秒内完成一笔记账，极简流程）
2. **消费结构透视**（AI LLM 分析消费结构）
3. **本地优先存储**（SQLite + 文件备份，隐私第一）
4. **钱迹数据导入**（CSV 格式导入，历史数据无缝衔接）

### 透视的具体定义

V1 聚焦交付：**优化建议**

| 功能 | V1 交付 | 说明 |
|------|---------|------|
| **优化建议** | ✅ | LLM 生成具体可执行的省钱建议（如"外卖每周减一次，月省 ¥200"）|
| **消费对比** | 后续版本 | 你 vs 同类人群（需外部数据支撑）|
| **趋势分析** | 后续版本 | 环比你的历史（需数据积累）|

### 数据获取方式

| 方式 | V1 支持 | 说明 |
|------|---------|------|
| 手动记账 | ✅ | 3秒快速记账 |
| 钱迹 CSV 导入 | ✅ | V1 必须功能，无缝迁移历史数据 |

---

## 三、第一个"哇"时刻 ⭐（v1.2 核心变更）

> **"攒到的就是赚到的"**

产品帮助用户攒下多少钱——这是给予用户的巨大正面反馈。

### 触发公式（明确版）

```
savedAmount = last_4_week_avg - current_week_spend
wow_triggered = savedAmount > ¥100 AND savedAmount > last_4_week_avg × 20%
```

### 详细说明

| 参数 | 定义 |
|------|------|
| `last_4_week_avg` | 用户历史最近4周消费均值（滚动计算，不足4周用默认值）|
| `current_week_spend` | 本周消费总额 |
| `savedAmount` | 节省金额（正数 = 少花了）|

### 触发条件（两者同时满足）

1. **绝对节省 > ¥100** — 避免 trivial win（少于 ¥100 不打扰用户）
2. **相对节省 > 历史均值 × 20%** — 确保有意义的进步

### 兜底规则

- 新用户（数据不足4周）：使用用户设定的周预算基准，或系统默认值
- 异常高消费周（如医疗紧急）：基准自适应，排除异常周
- 每周结算时间：**周日 20:00**，自动计算并推送通知

### 哇时刻展示内容

| 元素 | 说明 |
|------|------|
| 节省金额 | 金色超大字号 ¥XX |
| 原因描述 | "因为你少点了N次外卖，节省了 ¥XX" |
| 等价物 | "相当于：一顿双人火锅" |
| 成就徽章 | 🏆 精打细算 等 |
| 进度暗示 | "连续4周达标解锁 → 攒钱达人" |

### 不触发的情况

- 节省金额 ≤ ¥100 → 不展示
- 节省金额 ≤ 历史均值 × 20% → 不展示
- 消费 ≥ 上周（未节省）→ 不展示，沉默处理

---

## 四、不做的（保持聚焦）

- ~~社区功能~~
- ~~游戏化~~
- ~~饮食/健康管理~~
- ~~多平台同步~~
- ~~理财建议~~

> **V1 只做记账 + 透视 + 导入，极简出发。**

---

## 五、目标用户

- 有记账习惯的个人用户
- V1 最真实用户 = 产品负责人自己
